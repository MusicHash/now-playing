{
  "name": "now-playing-monorepo",
  "private": true,
  "workspaces": ["server", "client"],
  "scripts": {
    "start": "npm start -w server",
    "dev:server": "npm run debug -w server",
    "dev:client": "npm run dev -w client",
    "build:client": "npm run build -w client",
    "test": "npm test -w server"
  }
}
