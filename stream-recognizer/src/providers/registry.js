/**
 * Recognition layers (first success short-circuits where applicable).
 * Orchestration is implemented in {@link ../pipeline/orchestrator.js}.
 */
export const PROVIDER_ORDER = ['acrcloud', 'shazam', 'acoustid'];
